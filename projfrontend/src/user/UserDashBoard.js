import React from 'react'
import Base from "../core/Base"
const UserDashBoard =()=>{
    return (
        <Base title="Welcome helpMe" description="Admin Dashboard">
        <h5>Wait For ADMIN's Grant.....</h5>
        </Base>
    )
}

export default UserDashBoard 